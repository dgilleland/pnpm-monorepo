# Count Up

A simple TypeScript library for converting numbers to words.

## Installation

```bash
npm install count-up
# or
yarn add count-up
# or
pnpm add count-up
```

## Usage

This library provides functionality to convert numbers to their word representations.

```typescript
// Example usage will be added once the implementation is complete
```

## Development

This package is part of a pnpm monorepo. To develop:

1. Install dependencies: `pnpm install`
2. Build: `pnpm build`
3. Watch mode: `pnpm dev`

## License

ISC